<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boardMapper">
   <!-- 게시판에 글 작성하기 -->
     <insert id="insert">
	   INSERT INTO mp_board(    bno 
	   					      , title 
	   					      , content 
	   					      , writer  )
	                 VALUES(    mp_board_seq.nextval
	                 		  , #{title}
	                 		  , #{content}
	                 		  , #{writer}  );
   
   </insert>
   
   <select id="list" resultType="com.jihoon.vo.BoardVO">
		SELECT   bno
			   , title
			   , writer
			   , regdate
		  FROM mp_board
		  ORDER BY bno DESC;
	</select>
</mapper>